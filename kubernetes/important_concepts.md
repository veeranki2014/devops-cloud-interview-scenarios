1) EKS Acess entires vs aws-auth config
2) VPA, HPA, CA (Cluter Autoscaler), KEDA, Karpenter
3) Priority Classes for PODs
4) Admission Controllers
5) Docker: Entrypoint["sleep"],CMD["10"] = Kubernetes: command ["sleep"], args ["10"]